# 逐层 Δh 簇错误率：首轮已完成

## 这次确实按直接查表做

每层独立GMM → 每个簇的训练token错误比例 → 测试token查表。没有跨层贝叶斯、转移模型、熵融合、重新拟合GMM或题目等权。q未经平滑，是原始错误token数/总token数。五层均32簇，全部160簇有训练token支持，没有使用空簇回退。

原3011道训练题共 **1,613,606个token** 用于估计q。全部5000道题的 **2,690,319个token** 已保存逐层簇编号、当前token的q和同层累计平均。训练token总体错误来源比例为 **0.66524**；这是按token计数的参考比例，不等于按题错误率。簇表中的q不是“这个步骤算错的概率”。

GMM本身复用旧地图：每层从3011题各抽4个token，共12044个向量拟合，不是此次对全部token重训。只覆盖0→1、6→7、13→14、20→21、27→28五个层对，末层pre-RMS。没有覆盖全部28层。

## 1. 单个token与可见前缀平均分别评估

在第64个生成token时，986道仍在继续的历史测试题，目标为最终回答错误。AUROC越高表示越能把最终错误回答排到高风险侧。

| 层对 | 只看当前token的q | 同层前64个token的q平均 | 平均分数AUROC的95%区间 |
|---|---:|---:|---|
| 0→1 | 0.5269 | 0.5699 | [0.5340,0.6054] |
| 6→7 | 0.5591 | 0.6440 | [0.6086,0.6791] |
| 13→14 | 0.5598 | 0.6352 | [0.6000,0.6683] |
| 20→21 | 0.5629 | 0.6717 | [0.6388,0.7036] |
| 27→28 | 0.5656 | 0.6446 | [0.6088,0.6765] |

第128个token时，同层前缀平均AUROC依次为0.6185、0.6637、0.6728、0.7039、0.6844；此时测试集为974道仍在继续的问题，与前64token集合不同。不能将曲线当成同一人群的确定性提高。第一个生成token的五层分数AUROC约0.478–0.511，目前没有清楚的首token区分。

这里的平均是对q这些标量求平均，不是对hidden state或Δh向量求平均后聚类。每个向量始终单独进入GMM。平均分数也未校准成整题出错概率，不使用0.5阈值宣称分类准确率。

## 2. 簇本身确实有不同的错误来源比例

例如第14层，以下是训练比例最低/最高的簇，仅作完整32簇表中的描述性例子，不是根据它们另训练或选择策略：

| 第14层簇 | 训练错误比例 | 训练token数 | 测试错误比例 | 测试token数 |
|---|---:|---:|---:|---:|
| 29 | 46.77% | 33,886 | 48.26% | 10,967 |
| 15 | 87.28% | 156,158 | 89.76% | 49,421 |

这些簇在训练和测试上都表现出错误来源比例差异，说明按簇查表并非没有信息。但某个簇仍同时接收大量正确和错误回答的token；结果不是步骤错误标注，不能直接解释为进入簇15就执行了错误计算。完整160簇计数、不同来源问题数及训练/验证/测试比例均已导出。

## 计算、校验和保存

- 源码 **5515bd6**，本地与Lambda四项测试通过，包括训练标签隔离、层间独立、前缀无未来依赖及端到端合成验证。
- 正式CPU进程478047，计算和自动报告 **60.14秒**；其余时间是实现、测试和页面检查。无GPU占用、无新生成。
- 独立检查5000道题的计数、13,451,595个token×layer查表值、249,360项前缀分数；50个指标的100,000次bootstrap AUROC用独立的正负排序对比较法复算一致。
- 105个报告文件hash/HTTP、108个HTML引用通过；实际查看汇总图、簇热图和浏览器首页/逐题页；滑块位置1/64/246逐格对照原JSON通过，无浏览器错误。
- 统计区间按问题bootstrap、固定q，不重训地图/查表，逐项区间无多重比较校正。历史自动评分和反复探索的数据限制保留。没有据测试AUROC翻转分数或选择层后宣称新确认结果。

远端根目录 `/lambda/nfs/dami/hss/delta-cluster-lookup-20260925`；本地轻量目录 `results/delta-cluster-lookup-20260925`。全量逐token数组保留Dami；报告、160簇表、逐题前缀分数、q及五个原GMM已同步本地。

报告：<http://127.0.0.1:8793/report/index.html>；例题滑块：<http://127.0.0.1:8793/report/math_3958.html>。可选位置查看token文本、每层簇编号、q及同层累计平均。

summary SHA `ae8ad3905fca3cf554dc620db1af298fc4c486587c63b3f13ae026cd00f295b0`；lookup SHA `6193c146628e457687c2eb23953696bf36b5d96b15cdb08a6fb034d11f2fb6e2`；plan SHA `32843448010e80752cf1921aef07e2a5187cf22be7a76e0f65faa528d919ccea`。独立delivery.json代表页面检查完成，不改写冻结report或原队列历史字段。

本轮授权范围全部完成。新token向量可调用 scripts/delta_cluster_lookup_common.py 的 score_layer_deltas，用该层冻结GMM.predict之后查q；本轮实际评估仍是已保存回答的前缀回放，尚未接入实时生成或开展steering。
