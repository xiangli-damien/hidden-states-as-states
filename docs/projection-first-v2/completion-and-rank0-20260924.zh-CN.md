# v2 计算完成与 rank0 补充建议

**14:46 UTC 更新：** 182 对自动翻转现已全部完成 AI 阅读核验，详见 [逐题核验记录](semantic-review-complete-20260924.zh-CN.md)。下文 13:53 的“134 对未读”是历史状态。全条件评分修正仍待完成；没有启动 rank0 新实验。

## 当前完成范围

2026-09-24 13:53:09 UTC，原定 v2 队列完成所有 640 次新增生成、11 批原始审计、汇总、独立统计审计与报告。没有扩大候选搜索。当前 GPU 队列已结束；语义复核仍有待完成，不能把计算完成写成全部研究交付完成。

统计审计覆盖 2,336 条复用及新增记录、28 项比较、182 对自动评分翻转。summary SHA256 为 `b3f46f4bd02031f40ce0a3963b66dfd0daaf8fc231030249c869a3c7458637e2`。原主测试的统计与选择保持不变。

| 数据与比较 | 对照正确数 | 干预正确数 | 解读 |
|---|---:|---:|---|
| MATH 原主确认 256，local8 对 baseline | 129 | 130 | 未确认稳定纠错收益；原区间见主报告 |
| 新 GSM8K 64，local8 对 baseline | 62 | 61 | 差 −1.5625 个百分点，95% 区间 [−6.25, 3.125] |
| 新 GSM8K 64，shared8 对 baseline | 62 | 60 | 没有改善点估计 |
| 新 GSM8K 64，wrong-local8 对 baseline | 62 | 60 | local8 相对它的优势也尚未确认 |
| 原 MATH 验证子集 32，local16 对原 local8 | 20 | 16 | 探索性比较，不能推断 rank8 为最优维度 |

以上全部来自冻结自动评分器。已有原 MATH 主确认与验证集翻转核验发现提取/匹配错误，故不能将这些计数当作已经全面语义纠正的准确率。原评分保存用于复现；后续评分改进必须版本化、覆盖全部条件与问题，不能只修正干预获益的个案。原 64 题验证的 15 对、原主确认的 33 对已经实际阅读并封存 AI 核验；不是独立人工盲审。v2 的其他 134 对还没有全部阅读。

新 GSM8K baseline 达到 62/64，纠错机会很少；没有观察到提升既不证明等效，也不证明所有几何干预都无效。MATH 十项消融使用原验证题，逐项区间没有多比较校正；不能从这些探索中重新挑赢家后称其已通过主确认。

报告：<http://127.0.0.1:8787/report/index.html>。已实际查看 PNG 与渲染 PDF；569 个报告文件的 SHA、954 条本地资源引用、567 个 HTML 的 HTTP 内容匹配均通过。语义审核状态见独立 delivery sidecar，保留原报告收据不改。

## 新附件应如何落实

附件中“主测试还在运行”的时态已经过期。其概念区分成立：rank8 不是必需的维数，也未被证明为推理本征维度。当前 decoder 使用相同 GMM 锚点与局部残差 SVD 基；local8 额外保留当前样本的八个连续坐标，不是仅靠 state ID。

推荐的独立诊断保留原 Qwen、block14、自然前缀16、末4位置、greedy、2048 token 上限，以及相同 GMM 锚点：

1. 当前区域 centroid 完整替换：`h' = a_s`。被替换位置不保留簇内连续坐标。
2. 当前区域 centroid 小幅回拉：`h' = 0.7 h + 0.3 a_s`。它仍保留完整实例信息，不能称为 ID-only 压缩。
3. 复用原64验证题的 baseline 与 local8 回答，检验新增连续坐标的作用。
4. 若需要完整的同题 shared8 对照，须另外生成64条：原64验证只包含 baseline/C1/C2，没有 shared8。主确认256题与新GSM64里的 shared8 不能冒充同题对照。

因此两项 centroid 条件为128次新增生成；加入匹配的 shared8 则共192次。这个方案是独立的探索诊断，不修改已完成 v2 的640次范围，不重新命名主确认结果。最终仍分别报告行为保留、修复/损伤、截断与净正确率。centroid-only 对几个位置的成功，最多支持上下文条件下这些位置的可替代性，不能推出整个计算只需离散 ID。

截至本条记录，已通过异步问题询问用户是否补跑此诊断，尚未收到范围选择；没有启动新增 GPU 工作。先完成已经授权的报告核验及评分修正研究，不把无回复当成新增运行批准。
