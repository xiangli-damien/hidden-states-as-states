# 评分 v2：终结段落与精确整数

独立保留 v1 及所有原生成结果，不修改冻结代码。新版本仍是事后评分敏感性分析，规则已受之前案例启发，不是独立设计的新主指标。

## 在全量运行前固定的变化

- 提取不读取参考答案、条件或标签；优先完整 boxed，其次 Final Answer/Conclusion 段落、最后段落中真正引出数值或数学式的谓语。避免把几何名称、Mr.、较早推导里的 Therefore 当成最终答案。
- 存在多个并列最终值时保留整个列表，不能挑其中一个碰巧匹配参考。
- 精确整数必须相等；非整数数值保留原0.1%相对容差及1e-6绝对容差。进制下标只统一格式，不抹掉基数。
- 重算原 manifest 全部2,336条、全部30项对照。原科学配置、选优、划分、分数与 v1 均不改变。
- 只有题目全文、参考、回答全文哈希都相同才复用旧阅读。所有改变标签但未曾阅读的不同全文导出隐藏方法的阅读包；实际阅读后另存判断，不能把生成阅读包当作已核验。
- 原数学统计独立复算使用不导入提取器的审计脚本。问题级2000次 bootstrap，seed9242026；评分误差不在该区间内。

源代码：`projection_rescore_v2.py`、`run_projection_scoring.py`；复用的 v1 工具也逐个记录源码 SHA。输出 `scoring_sensitivity_v2/`。没有 GPU 生成、环境升级或外部判题 API。
