# 局部投影干预：MATH 256 题确认与评分诊断

截至 2026-09-24 12:25 UTC。原主实验 1,536 条生成全部完成，独立算子/原始记录/自动判分复算及问题级统计审计通过。报告入口：<http://127.0.0.1:8785/semantic_review_primary/index.html>。

## 冻结自动评分结果

固定 Qwen2-7B-Instruct，block14，先自然生成16个token，在全前缀重放时修改最后4个回答位置；当前区域 local8 投影，alpha=1，随后移除hook自由生成。该配置在原64题验证后、任何本次测试生成前冻结。

| 方法 | 正确率 | 相对baseline修复 | 损伤 | 截断 |
|---|---:|---:|---:|---:|
| Baseline | 129/256 = 50.39% | — | — | 8/256 |
| Local8 | 130/256 = 50.78% | 17 | 16 | 6/256 |
| Same-anchor shared8 | 127/256 = 49.61% | 17 | 19 | 13/256 |
| 径向/能量匹配随机，三个seed题内平均 | 49.35% | 11.67 | 14.33 | 2.86% |

Local8−baseline为 **+0.39个百分点，95%区间[-3.91,+4.69]**；对shared8为+1.17[-3.91,+6.25]，对随机均值为+1.43[-2.08,+5.08]。沿用冻结问题级bootstrap；逐项区间，没有多比较总体显著性声明。三个seed不是768道独立题。

**原验证集+7/64没有在这批确认题上复现。** 当前不支持稳定提升正确率，也不能据跨零区间证明严格等效。不能将先前参考续写KL的收益替换成自由生成纠错收益。历史MATH仅对本次方法选择留出，不声称问题从未参与项目。

## 完整阅读33个主比较翻转后发现的问题

Codex AI读取全部33对完整回答，方法和自动标签在A/B材料中隐藏；逐题笔记封存后才揭示对应关系。不是独立人类复核，也没有把代码产生的格式标记当成语义阅读。

**8/33对中，双方最终答案实际都正确，自动正误差来自答案提取。** 其中6个被自动算为修复，2个算为损伤。

| 题号 | 自动翻转 | 错误提取示例 |
|---|---|---|
| math_1019 | 损伤 | 正确结论672美元，提取到后面的10年 |
| math_1977 | 修复 | 正确表达式sqrt(3)/3被取成末尾3 |
| math_2628 | 修复 | 双方结论64，提取到“fying the previous conditions” |
| math_3441 | 修复 | 正确443_5被取成进制下标5 |
| math_4197 | 修复 | 正确402，误从list中的is取得t |
| math_438 | 修复 | 正确55，取到前面表达式的10 |
| math_4651 | 修复 | 正确(1+sqrt(5))/4被取成4 |
| math_4686 | 损伤 | 正确列向量(2,3,1)被取成1 |

从这33个已检查翻转中移除上述8对后，剩余11个最终答案修复、14个损伤。**这不是全256题重评分结果**：自动标签未翻转的223对也可能存在漏判，不能用局部核查给出新的准确率或置信区间。另外，部分正确最终答案伴随错误推导（如周期误用26仍碰巧得到正确8），不能将正确终值全部称为正确推理。

技术原因已定位到 `openact_core/tasks/parsers/math_parser.py`：结论正则的is缺词边界；结论候选强制取末尾数字会丢数学结构；没有boxed时可能优先取到早期结论片段。parse_failed=False只说明返回了非空字符串，不保证抽取的是最终答案。原审计复算的是同一个冻结评分器，保证可重复，但没有消除这类系统错误。

## 不改变当前科学运行

原始回答、自动分数、选择、统计和所有冻结源码保持原样。后续如做完整重评分，应在新目录中使用固定版本、覆盖全部问题/条件，同时保留原分数；不得只修改善结论的案例，也不能重新挑alpha后称原测试仍独立。当前v2继续原协议，不能在运行中改评分器。

新GSM8K64×4已完成生成和原始审计，10项验证消融顺序运行中；扩展结果尚未完成整体统计和案例复核，不在此文件抢先解释。完成后还需把本次发现纳入v2报告的评分限制，匹配case_id复用这33份阅读笔记，其余case仍须逐对阅读。

## 可复现产物与交付

- 原始根目录：`/lambda/nfs/dami/hss/revision-completion-20260924`。
- `steering_summary.json` SHA256：`c22e0fae0b5810b9281c25ab915996dba89d6a452aaea92cea853ee5039e81aa`。
- `paper_primary/`：读取冻结统计的PNG/PDF及主比较方法匿名材料。生成脚本`report_projection_primary_delivery.py`不重新拟合或重算区间。
- `semantic_review_primary/notes_blinded.json`：33份方法匿名阅读笔记，SHA256 `11c3b28eca5f3783b23accddee61e051b5550bac3ebdf383157afdf3c7602abb`。
- `semantic_review_primary/summary.json`及`cases/`：封存后揭示的方法、自动提取值、AI诊断和双方原文。生成脚本`report_projection_primary_review.py`。
- `delivery_primary.json`独立记录图表/HTTP核查及交付范围，不更改原_SUCCESS和visual_review_pending历史字段。

已实际查看PNG和渲染PDF，网页已截图核验。运行中的v2源码/输入SHA仍保持冻结，单GPU运行；当前阶段不扩大搜索。
