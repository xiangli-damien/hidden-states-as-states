# 同题采集审计恢复：在原 GPU 精确重放 RMSNorm

896条回答已经全部生成并封存，正式采集耗时11,743.83秒。原CPU全量审计失败：一个64-token数组的229,376个元素中，某一元素保存值1.65625、CPU重算1.671875，超出原来的`rtol=1/128, atol=.002`。原采集及后续队列因此正确停止，读出拟合尚未开始。

初步诊断是CPU/GPU的float32 reduction或rsqrt细微差异，经bf16归一化值舍入和gamma乘法再次舍入后放大。**该解释须由精确重放验证，不能仅凭“小误差”宣布数据正确。** 原CPU审计、日志、队列失败状态和采集源码均保留。

## 恢复标准

新增`revision_sameprompt_rms_replay.py`仅加载已保存的RMS权重，在原CUDA设备按已安装Qwen2RMSNorm的相同顺序计算：float32平方均值、rsqrt、乘原向量、转bf16、乘bf16 gamma。输入保持原始完整序列形状，把保存位置的真实pre-RMS状态放回原位置，其余行补零；RMSNorm逐行计算，其他token不影响目标行。要求与保存的post-RMS值**逐元素完全相等**。没有放宽原容差，也没有修改保存激活。

先做真实CUDA/HF RMSNorm检查：其他行保持非零时的输出与补零重放完全相等，覆盖prompt-last、16/64位置，且明确拒绝一处被修改的输出。通过后，新增`audit_revision_sameprompt_cuda.py`重新执行原来的全部SHA/身份/角色/判分/窗口/均值/熵/margin审计，仅以严格同设备重放替代跨设备近似RMS检查，并保存全部CPU超阈值位置对应的文件和计数。

## 队列与证据

新的独立恢复命名空间：原根目录下`recovery_rms_cuda_20260924/`，保存计划、日志、状态。它核对并冻结原失败状态及所有原源码SHA，确认旧进程全部退出。它不会再次生成答案，也不会重新运行已经完成的拟合。

只有全量新审计通过，才首次运行原封存的CPU读出、读出审计、报告和统计审计代码。兼容收据`full/audit.json`此前不存在，新审计才允许创建，且明确标明新审计来源。原`queue_status.json`和两份后续失败状态不改写为成功；后续进度以恢复目录中的`queue_status.json`为准。

任何精确重放不一致都停止恢复，保留全部证据，不继续放宽条件或发布预测结论。最终真实报告仍需视觉和原文检查。
