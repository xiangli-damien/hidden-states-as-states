# 最后一轮：已完成部分与正在运行的干预

更新：2026-09-24 05:16 UTC。结果目录`/lambda/nfs/dami/hss/revision-completion-20260924`。

[查看重构、迁移与样本流报告](http://127.0.0.1:8785/report_abd/index.html)。[完整执行协议](revision-completion-plan-20260924.zh-CN.md)。

## A：功能重构已经可以收口

固定GMM中心、block14、生成16token后、width16；复用已审计结果，未重跑模型。

| 数据 | 仅中心 KL | 中心＋local8 KL | 解释 |
|---|---:|---:|---|
| MATH历史测试32题 | 4.610 | 1.874 | 局部坐标改善参考输出保真 |
| GSM8K确认64题 | 5.402 | 2.893 | 冻结MATH decoder在目标数据上仍有功能价值 |

GSM8K local8−shared8的KL差为−1.409，95%区间[−2.131,−0.753]；参考续写NLL也支持local8。共享64维仍更好，因此不能宣称局部8维不可替代，不能把保真直接解释为纠错。主表使用同一锚点定义，经验中心FA/MFA是另一个对照协议。

## B：真正的回答前冻结迁移

使用Qwen2-7B-Instruct，冻结MATH训练的四层[7,14,21,28] **prompt-last** 地图与NB计数，先验仅计一次。原始GMM中心的区域分配沿用既有**最近中心**规则，不改为后验MAP；表示原样使用，无归一化。旧0.595/0.600对应prefix16，不是本表。

原536题确认集有466正确、70错误：

| 方法 | AUROC | 95%问题bootstrap区间 |
|---|---:|---|
| 完全冻结四层HSS-NB | 0.6156 | [0.5533,0.6743] |
| 完全冻结末层连续线性基线 | 0.7208 | [0.6562,0.7822] |
| 固定地图＋128条目标标签重新计数 | 0.5810 | [0.5070,0.6565] |

全部1,319题描述性统计：冻结NB 0.6099[0.5711,0.6482]，冻结线性基线0.6680[0.6280,0.7093]。模型原始答题正确率1134/1319=85.97%。这不是用NB判断正误的准确率，不能把类别不平衡下的高accuracy当检测收益。

支持的结论是**现有四层冻结状态计数有一定跨数据集排序能力**；本轮没有显示它优于连续基线，128题适配也没有显示提升。模型、提示模板和表示位置均已核对，无新增GSM生成或地图拟合。原确认集在历史实验中已分析，不称全新确认结果。

B2的128题按固定SHA从原541题适配池选取，与536确认题完全分离；来自官方test的历史划分，不是官方train，不算零样本。没有对目标标签选层、翻转分数、删远距离样本。

几何支持范围确有变化。按MATH验证集距离95%分位阈值，GSM确认题在block7/14/21/28内的覆盖分别为96.3%/12.9%/49.4%/42.4%。这是源与目标表示分布偏移的描述；不能据此直接断言为什么预测下降，也不能据此剔除样本后重算主指标。

## D：几何匹配确实对应部分持出样本流

复用训练中心的cosine Hungarian匹配，使用986道MATH持出题。原四层版本没有新增eta过滤；不是所有相邻层，也不是用test流优化匹配。

| block对 | 实际进入几何匹配后继区域 | 边际占用机会水平 |
|---|---:|---:|
| 7→14 | 31.85% | 3.23% |
| 14→21 | 20.99% | 3.09% |
| 21→28 | 41.68% | 3.27% |

这支持几何配对具有样本对应关系，不能升级为相同功能或因果身份。新增报告复核了冻结匹配、逐题assignment和实际边际分母。

## C：真实答题干预正在运行，尚无改善结论

GPU监督453932，smoke子进程453967；CPU队列453933已完成。05:16UTC已写出17/56个smoke条件，覆盖3题。吞吐约41token/s，已完成条件平均16.0秒、平均681生成token；样本仍少，只用于初步时间预算。

- 小Qwen真实cache/identity测试通过，修改后的cache与完整重放下一步logits最大误差2.24e−8。
- 本地及远端5项测试通过，包含合成选择/题内seed平均/独立统计及篡改拒绝。合成数据不是LLM科学结果。
- 8题真实smoke全部完成并独立审计后，才继续完整64题验证；当前不能宣称smoke已经通过。
- 预计验证阶段还有约2–4小时。若没有正净收益候选，在验证后停止；若有，256题候选与必要对照再约6–12小时，另外预留审计和图表时间。按当前少量实测，完整分支暂估10–18小时，回答长度可能改变预计。
- 硬截止为2026-09-26 05:10UTC（纽约01:10）；计算在截止前6小时停止，第22小时以前决定是否进入测试。自动队列与每30分钟监控均已配置。

## 审计与保存

CPU分析126.64秒，原始权重/计数/预测复算与独立rank-AUROC、区间、匹配点估计审计通过。图表PNG与渲染PDF已实际检查，4个本地链接和HTTP页面字节核对通过。

报告收据SHA `ea32fbad9f825bb16e06a6b3e17b715389b7f8ab25d12275828978367605213f`；CPU审计SHA `cb578ab21793e2040cc94d0553a3e47abae58cf437f09bb2782b3bf7db70cc5e`。`delivery_abd.json`只表示A/B/D交付，绝不表示C已完成。源、结果、逐题预测与图表均保留；完整干预输出在dami，后续轻量报告同步本地。
